# react-grab-anywhere-injector
A lightweight ViolentMonkey/TamperMonkey userscript that automatically injects React Grab into any webpage (including localhost) so you can instantly grab elements with ⌘C / Ctrl+C + click
